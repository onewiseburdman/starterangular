import { FontfamilyDirective } from './fontfamily.directive';

describe('FontfamilyDirective', () => {
  it('should create an instance', () => {
    const directive = new FontfamilyDirective();
    expect(directive).toBeTruthy();
  });
});
